name: first-workflow
on: push
jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: say hello
        run: echo "Hello from github actions"

      - name: show current directory  
        run: pwd

      - name: list files
        run: ls
        